namespace Api.Tests;

/// <summary>
/// Placeholder test so the CI pipeline (Story 1.3) has something real to build and run.
/// Replace/extend with real coverage as features land in <c>src/Api</c>.
/// </summary>
public class PlaceholderTests
{
    [Fact]
    public void Pipeline_Placeholder_Passes()
    {
        Assert.Equal(2, 1 + 1);
    }
}
